// EXAMPLE/FIXTURE ONLY -- illustrates the shape
// `python -m sales_ai.cli redirect-map-export` produces (as an ES
// module default export, matching redirect-map.js -- see that file's
// own comment for why a .js module, not a .json import, is used).
// Every URL below is RFC 2606 reserved (example.invalid), never a real
// Rakuten affiliate link. This file is never imported by go.js itself;
// it exists only for local testing/documentation.
export default {
  "6": {
    "10": "https://example.invalid/fixture-affiliate-link"
  }
};
